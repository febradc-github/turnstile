---
description: Fast lane for trivial work and bug fixes. Creates a small task or story with inline acceptance criteria and adds it directly to the current sprint after one approval -- no design doc, no spec.
argument-hint: "[short description]"
disable-model-invocation: true
---

Use the Skill tool to invoke the `cadence-quick` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
