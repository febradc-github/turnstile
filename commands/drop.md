---
description: Cancels a ticket. Sets status dropped with a recorded reason, keeping the item visible as history instead of hand-deleting YAML.
argument-hint: "[id] [reason]"
disable-model-invocation: true
---

Use the Skill tool to invoke the `cadence-drop` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
