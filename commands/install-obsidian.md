---
description: Detects whether Obsidian is installed, installs it via the OS package manager if not, and scaffolds cadence/.obsidian/ so the brain opens as a working vault.
disable-model-invocation: true
---

Use the Skill tool to invoke the `cadence-install-obsidian` skill.
