---
description: Reads cadence/loops/<id>/state.json and renders the current phase, status, and full iteration history for the given loop run.
argument-hint: "<id>"
disable-model-invocation: true
---

Use the Skill tool to invoke the `cadence-loop-status` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
