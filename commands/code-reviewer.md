---
description: General, advisory, non-gated review of code or a diff for correctness bugs and reuse/simplification opportunities. Never commits and never touches ticket status -- distinct from the /cadence:review ticket-closing gate.
argument-hint: "[optional: files or scope to review]"
disable-model-invocation: true
---

Use the Skill tool to invoke the `cadence-code-reviewer` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
